# Matrix Builder Web

Next.js application for the public Matrix Builder experience.

Batch 1 keeps the UI as a buildable shell. Batch 2 converts the Scout `Matrix-Builder.html` prototype into the real page.
