# Scout Design Handoff

The Scout design prototype is preserved under `design-handoff/scout/`.

Batch 2 will convert `scout/Matrix-Builder.html` into the production Next.js page under `apps/web/src/app/matrix-builder/`.
